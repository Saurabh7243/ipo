export enum TypeEnum { //ipo table
  Mainbord = 1,
  SME = 2,
}

export enum ExchangedEnum { // ipo table
  NSE = 1,
  BSE = 2,
  BOTH = 3,
}

export enum IssueTypeEnum { // ipo table
  IPO = 1,
  FPO = 2,
}

export enum IpoTypeEnum { //  ipoDetail table page
  BOOK_BUILT_ISSUE = 1,
  FIXED_PRICE_ISSUE = 2,
}

export enum FetchDataEnum { //  ipo table page
  BSE = 1,
  NSE = 2,
}


export enum ServerUploadFileEnum { //  for ftp server
  LOGO = 1,
  BOA = 2,
  ANCHOR = 3,
  RHP = 4,
  DRHP = 5,

}